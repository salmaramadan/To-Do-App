<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>



    <application
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/note_alt_26"
        android:label="@string/app_name"
        android:roundIcon="@drawable/note_alt_26"
        android:supportsRtl="true"
        android:theme="@style/Theme.ToDo.Light"
        android:usesCleartextTraffic="true"
        tools:targetApi="31"
        >
        <receiver android:name=".NotificationReceiver" />

        <activity android:name="com.salma.todo.AddNoteActivity"/>

        <activity android:name="com.salma.todo.NoteActivity" />
        <activity
            android:name="com.salma.todo.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.ToDo.Light">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.salma.todo.GuideActivity" />
    </application>

</manifest>